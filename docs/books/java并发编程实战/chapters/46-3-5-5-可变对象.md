# 3.5.5 可变对象

如果对象在构造后可以修改，那么安全发布只能确保“发布当时”状态的可⻅性。对于可变对象，不仅在发布对象时需要使⽤同步，⽽且在每次对象访问时同样需要使⽤同步来确保后续修改操作的可⻅性。要安全地共享可变对象，这些对象就必须被安全地发布，并且必须是线程安全的或者由某个锁保护起来。

对象的发布需求取决于它的可变性：

·不可变对象可以通过任意机制来发布。  
·事实不可变对象必须通过安全⽅式来发布。  
·可变对象必须通过安全⽅式来发布，并且必须是线程安全的或者由某个锁保护起来。