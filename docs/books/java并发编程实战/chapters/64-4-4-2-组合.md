# 4.4.2 组合

当为现有的类添加⼀个原⼦操作时，有⼀种更好的⽅法：组合（Composition）。程序清单4-16中的ImprovedList通过将List对象的操作委托给底层的List实例来实现List的操作，同时还添加了⼀个原⼦的putIfAbsent⽅法。（与Collections.synchronizedList和其他容器封装器⼀样，ImprovedList假设把某个链表对象传给构造函数以后，客户代码不会再直接使⽤这个对象，⽽只能通过ImprovedList来访问它。）

**程序清单4-16 通过组合实现“若没有则添加”**

@ThreadSafe   
public class ImprovedList<T>implements List<T>{ private final List<T>list;   
public ImprovedList(List<T>list){this.list $\equiv$ list; public synchronized boolean putIfAbsent (T x）{ boolean contains $=$ list.contains(x）; if (contains)   
list.add(x);   
return!contains;   
}   
public synchronized void clear() {list.clear();} //.....按照类似的方式委托List的其他方法

ImprovedList通过⾃⾝的内置锁增加了⼀层额外的加锁。它并不关⼼底层的List是否是线程安全的，即使List不是线程安全的或者修改了它的加锁实现，ImprovedList也会提供⼀致的加锁机制来实现线程安全性。虽然额外的同步层可能导致轻微的性能损失 [1]，但与模拟另⼀个对象的加锁策略相⽐，ImprovedList更为健壮。事实上，我们使⽤了Java监视器模式来封装现有的List，并且只要在类中拥有指向底层List的唯⼀外部引⽤，就能确保线程安全性。

[1] 性能损失很⼩，因为在底层List上的同步不存在竞争，所以速度很快，请参⻅第11章。