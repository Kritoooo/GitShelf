# 4.3.1 ⽰例：基于委托的⻋辆追踪器

下⾯将介绍⼀个更实际的委托⽰例，构造⼀个委托给线程安全类的⻋辆追踪器。我们将⻋辆的位置保存到⼀个Map对象中，因此⾸先要实现⼀个线程安全的Map类，ConcurrentHashMap。我们还可以⽤⼀个不可变的Point类来代替MutablePoint以保存位置，如程序清单4-6所⽰。

程序清单4-6 在DelegatingVehicleTracker中使⽤的不可变Point类

```txt
public class Point{   
public final int x, y;   
public Point (int x, int y) { this.x=x; this.y=y; } 
```

由于Point类是不可变的，因⽽它是线程安全的。不可变的值可以被⾃由地共享与发布，因此在返回location时不需要复制。

在程序清单4-7的DelegatingVehicleTracker中没有使⽤任何显式的同步，所有对状态的访问都由ConcurrentHashMap来管理，⽽且Map所有的键和值都是不可变的。

程序清单4-7 将线程安全委托给ConcurrentHashMap

@ThreadSafe   
public class DelegatingVehicleTracker{   
private final ConcurrentMap<String, Point>locations;   
private final Map<String, Point>unmodifiableMap;   
public DelegatingVehicleTracker(Map<String, Point>points) { locations $\equiv$ new ConcurrentHashMap<String, Point> (points); unmodifiableMap $=$ Collections.unmodifiableMap (locations) ;

```java
}   
public Map<String, Point>getLocations(){ return unmodifiableMap;   
public Point getLocation (String id){ return locations.get (id) ;   
public void setLocation (String id, int x, int y){ if (locations.replace (id, new Point (x, y)) ==null) throw new IllegalArgumentException( "invalid vehicle name:" +id) ;   
1 
```

如果使⽤最初的MutablePoint类⽽不是Point类，就会破坏封装性，因为getLocations会发布⼀个指向可变状态的引⽤，⽽这个引⽤不是线程安全的。需要注意的是，我们稍微改变了⻋辆追踪器类的⾏为。在使⽤监视器模式的⻋辆追踪器中返回的是⻋辆位置的快照，⽽在使⽤委托的⻋辆追踪器中返回的是⼀个不可修改但却实时的⻋辆位置视图。这意味着，如果线程A调⽤getLocations，⽽线程B在随后修改了某些点的位置，那么在返回给线程A的Map中将反映出这些变化。在前⾯提到过，这可能是⼀种优点（更新的数据），也可能是⼀种缺点（可能导致不⼀致的⻋辆位置视图），具体情况取决于你的需求。

如果需要⼀个不发⽣变化的⻋辆视图，那么getLocations可以返回对locations这个Map对象的⼀个浅拷⻉（Shallow Copy）。由于Map的内容是不可变的，因此只需复制Map的结构，⽽不⽤复制它的内容，如程序清单4-8所⽰（其中只返回⼀个HashMap，因为getLocations并不能保证返回⼀个线程安全的Map）。

**程序清单4-8 返回locations的静态拷⻉⽽⾮实时拷⻉**

```java
public Map<String, Point>getLocations() { return CollectionsmodifiableMap ( new HashMap<String, Point>(locations)); } 
```

[1] 如果count不是final类型，那么要分析CountingFactorizer的线程安全性将变得更复杂。如果CountingFactorizer将count修改为指向另⼀个AtomicLong域的引⽤，那么必须确保count的更新操作对于所有访问count的线程都是可⻅的，并且还要确保在count的值上不存在竞态条件。这也是尽可能使⽤final类型域的另⼀个原因。