# 4.3.5 ⽰例：发布状态的⻋辆追踪器

我们来构造⻋辆追踪器的另⼀个版本，并在这个版本中发布底层的可变状态。我们需要修改接⼝以适应这种变化，即使⽤可变且线程安全的Point类。

程序清单4-11中的SafePoint提供的get⽅法同时获得x和y的值，并将⼆者放在⼀个数组中返回 [1]。如果为x和y分别提供get⽅法，那么在获得这两个不同坐标的操作之间，x和y的值发⽣变化，从⽽导致调⽤者看到不⼀致的值：⻋辆从来没有到达过位置（x, y）。通过使⽤SafePoint，可以构造⼀个发布其底层可变状态的⻋辆追踪器，还能确保其线程安全性不被破坏，如程序清单4-12中的PublishingVehicleTracker类所⽰。

**程序清单4-11 线程安全且可变的Point类**

```txt
@ThreadSafe   
public class SafePoint{ @GuardedBy("this") private int x,y; private SafePoint (int[a]a){this(a[0],a[1]) ;} public SafePoint (SafePoint p){this(p.get())；} public SafePoint (int x,int y){ this.x=x; this.y=y; } 
```

```txt
public synchronized int[] get() { return new int[]{x，y}；   
public synchronized void set (int x，int y){ this.x=x; this.y=y;   
1 
```

**程序清单4-12 安全发布底层状态的⻋辆追踪器**

```java
@ThreadSafe   
public class PublishingVehicleTracker{   
private final Map<String, SafePoint>locations;   
private final Map<String, SafePoint>unmodifiableMap;   
public PublishingVehicleTracker(   
Map<String, SafePoint>locations) {   
thisLocations   
= new ConcurrentHashMap<String, SafePoint> (locations);   
this.unmodifiableMap   
=Collections/unmodifiableMap (thisLocations); 
```

```java
}   
public Map<String, SafePoint>getLocations(){ return unmodifiableMap;   
public SafePoint getLocation (String id){ return locations.get (id) ;   
public void setLocation(String id, int x, int y){ if(!locations.containsKey(id)) throw new IllegalArgumentException( "invalid vehicle name:" +id) ; locations.get(id).set(x,y);   
} 
```

PublishingVehicleTracker将其线程安全性委托给底层的

ConcurrentHashMap，只是Map中的元素是线程安全的且可变的Point，⽽并⾮不可变的。getLocation⽅法返回底层Map对象的⼀个不可变副本。调⽤者不能增加或删除⻋辆，但却可以通过修改返回Map中的SafePoint值来改变⻋辆的位置。再次指出，Map的这种“实时”特性究竟是带来好处还是坏处，仍然取决于实际的需求。

PublishingVehicleTracker是线程安全的，但如果它在⻋辆位置的有效值上施加了任何约束，那么就不再是线程安全的。如果需要对⻋辆位置

的变化进⾏判断或者当位置变化时执⾏⼀些操作，那么PublishingVehicleTracker中采⽤的⽅法并不合适。

[1] 如果将拷⻉构造函数实现为this（p.x, p.y），那么会产⽣竞态条件，⽽私有构造函数则可以避免这种竞态条件。这是私有构造函数捕获模式（Private Constructor Capture Idiom, Bloch and Gafter，2005）的⼀个实例。