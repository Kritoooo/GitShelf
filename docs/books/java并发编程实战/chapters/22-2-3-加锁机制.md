# 2.3 加锁机制

当在Servlet中添加⼀个状态变量时，可以通过线程安全的对象来管理Servlet的状态以维护Servlet的线程安全性。但如果想在Servlet中添加更多的状态，那么是否只需添加更多的线程安全状态变量就⾜够了？

假设我们希望提升Servlet的性能：将最近的计算结果缓存起来，当两个连续的请求对相同的数值进⾏因数分解时，可以直接使⽤上⼀次的计算结果，⽽⽆须重新计算。（这并⾮⼀种有效的缓存策略，5.6节将给出⼀种更好的策略。）要实现该缓存策略，需要保存两个状态：最近执⾏因数分解的数值，以及分解结果。

我们曾通过AtomicLong以线程安全的⽅式来管理计数器的状态，那么，在这⾥是否可以使⽤类似的AtomicReference [1] 来管理最近执⾏因数分解的数值及其分解结果吗？在程序清单2-5中的UnsafeCachingFactorizer实现了这种思想。

程序清单2-5 该Servlet在没有⾜够原⼦性保证的情况下对其最近计算结果进⾏缓存（不要这么做）

@NotThreadSafe   
public class UnsafeCachingFactorizer implements Servlet{ private final AtomicInteger<BigInt>lastNumber $=$ new AtomicInteger<BigInt $\rightharpoondown$ （）; private final AtomicInteger<BigInt[]>lastFactors $=$ new AtomicInteger<BigInt[] $\rightharpoonup$ （）;

```java
public void service (ServletRequest req, ServletResponse resp) {   
    AtomicInteger i = extractFromRequest (req);   
    if (i.equals(lastNumber.get())));   
    encodeIntoResponse (resp, lastFactors.get());   
    else {   
        AtomicInteger[] factors = factor (i);   
        lastNumber.set (i);   
        lastFactors.set (factors);   
        encodeIntoResponse (resp, factors);   
    }   
} 
```

然⽽，这种⽅法并不正确。尽管这些原⼦引⽤本⾝都是线程安全的，但在UnsafeCaching Factorizer中存在着竞态条件，这可能产⽣错误的结果。

在线程安全性的定义中要求，多个线程之间的操作⽆论采⽤何种执⾏时序或交替⽅式，都要保证不变性条件不被破坏。

UnsafeCachingFactorizer的不变性条件之⼀是：在lastFactors中缓存的因数之积应该等于在lastNumber中缓存的数值。只有确保了这个不变性条件不被破坏，上⾯的Servlet才是正确的。当在不变性条件中涉及多个变量时，各个变量之间并不是彼此独⽴的，⽽是某个变量的值会

对其他变量的值产⽣约束。因此，当更新某⼀个变量时，需要在同⼀个原⼦操作中对其他变量同时进⾏更新。

在某些执⾏时序中，UnsafeCachingFactorizer可能会破坏这个不变性条件。在使⽤原⼦引⽤的情况下，尽管对set⽅法的每次调⽤都是原⼦的，但仍然⽆法同时更新lastNumber和lastFactors。如果只修改了其中⼀个变量，那么在这两次修改操作之间，其他线程将发现不变性条件被破坏了。同样，我们也不能保证会同时获取两个值：在线程A获取这两个值的过程中，线程B可能修改了它们，这样线程A也会发现不变性条件被破坏了。

要保持状态的⼀致性，就需要在单个原⼦操作中更新所有相关的状态变量。