# 程序清单13-5 可中断的锁获取操作

```java
public boolean sendOnSharedLine(String message) throws InterruptedException { lock.lockInterruptibly(); try{ 
```

```txt
return cancellableSendOnSharedLine(message); }finally{ lock.unlock(); 1 private boolean cancellableSendOnSharedLine(String message) throws InterruptedException {... } 
```