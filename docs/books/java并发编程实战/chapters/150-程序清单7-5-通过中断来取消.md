# 程序清单7-5 通过中断来取消

```txt
class PrimeProducer extends Thread { private final BlockingQueue<BigInt> queue; PrimeProducer(BlockingQueue<BigInt> queue) { this.queue = queue; public void run() { try { 
```

```java
BigInt p =BigInt ONE;
while (!Thread.currentThread().isInterrupted())
    queue.put(p = p.nextProbablePrime());
} catch (InterruptedException consumed) {
    /* 允许线程退出 */
} 
```